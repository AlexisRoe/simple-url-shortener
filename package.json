{
  "name": "simple-url-shortener",
  "version": "1.0.0",
  "description": "a simple url-shortener using Python and Redis",
  "keywords": [
    "python",
    "redis",
    "valkyr",
    "docker"
  ],
  "homepage": "https://github.com/AlexisRoe/simple-url-shortener#readme",
  "bugs": {
    "url": "https://github.com/AlexisRoe/simple-url-shortener/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AlexisRoe/simple-url-shortener.git"
  },
  "license": "MIT",
  "author": "Alexis Roehrling",
  "scripts": {
    "sync-version": "scripts/sync-version.sh",
    "docker:up": "npm run sync-version && docker compose --env-file .env -f infra/docker-compose.yml up -d --build",
    "docker:down": "docker compose --env-file .env -f infra/docker-compose.yml down",
    "docker:logs": "docker compose --env-file .env -f infra/docker-compose.yml logs -f"
  }
}